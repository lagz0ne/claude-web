Fetch https://main.vite.dev/guide/migration and compare against this project's `web/vite.config.ts` and `web/package.json`. Report:

1. Any deprecated config options we're using
2. Breaking changes that affect our dependencies (check `@vitejs/plugin-react`, `@tailwindcss/vite` peer dep compatibility)
3. New defaults we should be aware of
4. Whether our current vite beta version is latest (check `npm view vite@beta version`)

Keep the report concise — only flag actionable items.
